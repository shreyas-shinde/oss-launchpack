{
  "name": "oss-launchpack",
  "version": "0.1.0",
  "description": "Generate production-minded launchpacks for popular self-hosted OSS apps.",
  "type": "module",
  "bin": {
    "oss-launchpack": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test test/*.test.ts",
    "check": "pnpm build && pnpm test"
  },
  "keywords": [
    "open-source",
    "self-hosted",
    "docker-compose",
    "deployment",
    "managed-deployment",
    "oss"
  ],
  "author": "Shreyas Shinde",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
